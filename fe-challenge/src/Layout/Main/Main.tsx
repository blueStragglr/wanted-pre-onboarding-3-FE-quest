/** @jsxImportSource @emotion/react */
import { css } from "@emotion/react";

const Main = () => {
  return (
    <main
      css={css`
        width: 100%;
        height: calc(100vh - 30px);
        color: aquamarine;
      `}
    >
      this is main
    </main>
  );
};

export default Main;
