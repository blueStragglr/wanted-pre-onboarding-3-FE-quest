import React from 'react';
import $ from './sideBar.module.scss'

const SideBar = () => {
  return (
    <article>
      <ul>
        {
          Array(3).fill().map()
        }
      </ul>
    </article>
  );
};

export default SideBar;