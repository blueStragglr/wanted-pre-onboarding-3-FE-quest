import styled from "@emotion/styled";
import React from "react";

const Header = () => {
  return <StHeader>Wanted Pre-onboarding course</StHeader>;
};

export default Header;

const StHeader = styled.header`
  border-bottom: 1px solid black;
  padding: 20px;
`;
