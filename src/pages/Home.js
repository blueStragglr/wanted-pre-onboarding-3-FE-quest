import React from "react";

// CSS
import styled from "styled-components";

const PageWrapper = styled.div`
  margin: auto;
`;

const Home = () => {
  return (
    <PageWrapper>
      <h1>This is Home!</h1>
    </PageWrapper>
  );
};

export default Home;
