import styled from "styled-components";

const PageWrapper = styled.article`
  margin: auto;
`;

const Home = () => {
  return (
    <PageWrapper>
      <h1>This is Home!</h1>
    </PageWrapper>
  );
};

export default Home;
