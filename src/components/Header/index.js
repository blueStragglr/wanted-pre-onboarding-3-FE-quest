import styles from "./Header.module.css";

const Header = () => {
  return (
    <header>
      <h4 className={styles.h4}>Wanted Pre-onboarding course</h4>
      <hr />
    </header>
  );
};

export default Header;
