export const PATH_ROUTE = Object.freeze({
  root: '/',
  join: '/join',
  login: '/login',
  pagea: '/pagea',
  pageb: '/pageb',
  pagec: '/pagec',
});

export const PATH_API = Object.freeze({
  root: '/',
  login: '/login',
  join: '/register',
});
